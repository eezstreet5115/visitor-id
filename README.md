# visitor-id
Manage visitor data (like name, occupation, address, and password) using a MySQL database called visitor.

Prerequisites:
Must have MySQL and Python installed in pc, with mysql.connector module also installed.
